<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>
        <%= astronaut.name %> - Astronaut Details
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
    </style>
</head>

<body class="flex flex-col min-h-screen bg-gray-100">
    <header class="bg-blue-600 text-white p-4">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-3xl font-bold"><a href="/launches" class="hover:underline">Launch Library</a></h1>
            <nav>
                <ul class="flex space-x-6">
                    <li><a href="/launches" class="hover:underline">Launches</a></li>
                    <li><a href="/spacecrafts" class="hover:underline">Spacecrafts</a></li>
                    <li><a href="/astronauts" class="hover:underline">Astronauts</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <main class="container mx-auto my-6 p-6 bg-white shadow-lg rounded-lg" itemscope itemtype="http://schema.org/Person">
        <% if (astronaut) { %>
            <h2 class="text-4xl font-bold text-center text-blue-800 mb-6" itemprop="name">
                <%= astronaut.name %>
            </h2>

            <section class="flex flex-wrap -mx-4">
                <figure class="w-full lg:w-1/2 px-4 mb-6 lg:mb-0 text-center">
                    <% if (astronaut.profile_image) { %>
                        <img src="<%= astronaut.profile_image %>" alt="<%= astronaut.name %>"
                            class="w-64 h-64 mx-auto rounded-full object-cover shadow-lg" itemprop="image">
                        <% } else { %>
                            <div
                                class="w-64 h-64 mx-auto rounded-full bg-gray-200 flex items-center justify-center shadow-lg text-gray-500 text-lg"
                                role="img" aria-label="No image available for <%= astronaut.name %>">
                                No Image Available</div>
                            <% } %>
                </figure>

                <section class="w-full lg:w-1/2 px-4">
                    <article class="mb-4 p-4 border border-gray-200 rounded-md bg-gray-50">
                        <h3 class="text-xl font-semibold text-gray-700 mb-2">Astronaut Details</h3>
                        <p class="text-gray-700 mb-1"><strong>Age:</strong> <span itemprop="age"><%= astronaut.age %></span></p>
                        <p class="text-gray-700 mb-1"><strong>Status:</strong> <span itemprop="additionalType"><%= astronaut.status ? astronaut.status.name : 'N/A' %></span></p>
                        <p class="text-gray-700 mb-1"><strong>Type:</strong> <span itemprop="jobTitle"><%= astronaut.type ? astronaut.type.name : 'N/A' %></span></p>
                        <p class="text-gray-700 mb-1"><strong>Agency:</strong> <span itemprop="worksFor" itemscope itemtype="http://schema.org/Organization"><span itemprop="name"><%= astronaut.agency ? astronaut.agency.name : 'N/A' %></span></span></p>
                    </article>

                    <article class="p-4 border border-gray-200 rounded-md bg-gray-50">
                        <h3 class="text-xl font-semibold text-gray-700 mb-2">Biography:</h3>
                        <p class="text-gray-700" itemprop="description">
                            <%= astronaut.bio || 'No biography available.' %>
                        </p>
                    </article>
                </section>
            </section>

            <aside class="text-center mt-8">
                <a href="/astronauts"
                    class="inline-block bg-blue-500 hover:bg-blue-600 text-white font-bold py-3 px-6 rounded-full transition duration-300 shadow-lg">Back
                    to Astronauts</a>
            </aside>

            <% } else { %>
                <section class="text-center py-12">
                    <p class="text-xl text-gray-600">Astronaut details could not be loaded.</p>
                    <a href="/astronauts"
                        class="mt-4 inline-block bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded-full">Back
                        to Astronauts</a>
                </section>
                <% } %>
    </main>

    <footer class="bg-gray-800 text-white text-center p-4 mt-auto">
        <p>&copy; 2025 Marcell Nemes</p>
    </footer>
</body>

</html>