<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>
        <%= launch.name %> - Launch Details
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
    </style>
</head>

<body class="flex flex-col min-h-screen bg-gray-100">
    <header class="bg-blue-600 text-white p-4">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-3xl font-bold"><a href="/launches" class="hover:underline">Launch Library</a></h1>
            <nav>
                <ul class="flex space-x-6">
                    <li><a href="/launches" class="hover:underline">Launches</a></li>
                    <li><a href="/spacecrafts" class="hover:underline">Spacecrafts</a></li>
                    <li><a href="/astronauts" class="hover:underline">Astronauts</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <main class="container mx-auto my-6 p-6 bg-white shadow-lg rounded-lg" itemscope itemtype="http://schema.org/Event">
        <% if (launch) { %>
            <h2 class="text-4xl font-bold text-center text-blue-800 mb-6" itemprop="name">
                <%= launch.name %>
            </h2>

            <section class="flex flex-wrap -mx-4">
                <figure class="w-full lg:w-1/2 px-4 mb-6 lg:mb-0">
                    <% if (launch.image) { %>
                        <img src="<%= launch.image %>" alt="<%= launch.name %> launch image"
                            class="w-full h-auto rounded-lg shadow-md object-cover max-h-96"
                            itemprop="image">
                        <% } else { %>
                            <div
                                class="w-full h-64 bg-gray-200 flex items-center justify-center rounded-lg shadow-md text-gray-500"
                                role="img" aria-label="No image available for <%= launch.name %>">
                                No Image Available</div>
                            <% } %>
                </figure>

                <section class="w-full lg:w-1/2 px-4">
                    <article class="mb-4 p-4 border border-gray-200 rounded-md bg-gray-50">
                        <h3 class="text-xl font-semibold text-gray-700 mb-2">Launch Details</h3>
                        <p class="text-gray-700 mb-1"><strong>Date:</strong>
                            <time datetime="<%= launch.net %>" itemprop="startDate"><%= new Date(launch.net).toLocaleString() %></time>
                        </p>
                        <p class="text-gray-700 mb-1"><strong>Status:</strong>
                            <span itemprop="eventStatus"><%= launch.status.name %></span> (<%= launch.status.description %>)
                        </p>
                        <p class="text-gray-700 mb-1"><strong>Provider:</strong>
                            <span itemprop="organizer" itemscope itemtype="http://schema.org/Organization"><span itemprop="name"><%= launch.launch_service_provider.name %></span></span>
                        </p>
                        <% if (launch.rocket && launch.rocket.configuration) { %>
                            <p class="text-gray-700 mb-1"><strong>Rocket:</strong>
                                <span itemprop="performer" itemscope itemtype="http://schema.org/Person"><span itemprop="name"><%= launch.rocket.configuration.full_name %></span></span>
                            </p>
                            <% } %>
                                <% if (launch.pad && launch.pad.name && launch.pad.location) { %>
                                    <p class="text-gray-700 mb-1"><strong>Launch Pad:</strong>
                                        <span itemprop="location" itemscope itemtype="http://schema.org/Place"><span itemprop="name"><%= launch.pad.name %></span>, <span itemprop="address" itemscope itemtype="http://schema.org/PostalAddress"><span itemprop="addressLocality"><%= launch.pad.location.name %></span></span></span>
                                    </p>
                                    <% } %>
                    </article>

                    <% if (launch.mission) { %>
                        <article class="p-4 border border-gray-200 rounded-md bg-gray-50" itemprop="workPerformed" itemscope itemtype="http://schema.org/CreativeWork">
                            <h3 class="text-xl font-semibold text-gray-700 mb-2" itemprop="name">Mission Overview</h3>
                            <p class="text-gray-700 mb-2"><strong>Name:</strong>
                                <%= launch.mission.name %>
                            </p>
                            <p class="text-gray-700 mb-2"><strong>Type:</strong>
                                <span itemprop="genre"><%= launch.mission.type %></span>
                            </p>
                            <p class="text-gray-700 mb-2"><strong>Orbit:</strong>
                                <span itemprop="about"><%= launch.mission.orbit ? launch.mission.orbit.name : 'N/A' %></span>
                            </p>
                            <p class="text-gray-700"><strong>Description:</strong>
                                <span itemprop="description"><%= launch.mission.description %></span>
                            </p>
                        </article>
                        <% } %>
                </section>
            </section>

            <aside class="text-center mt-8">
                <a href="/launches"
                    class="inline-block bg-blue-500 hover:bg-blue-600 text-white font-bold py-3 px-6 rounded-full transition duration-300 shadow-lg">Back
                    to Launches</a>
            </aside>

            <% } else { %>
                <section class="text-center py-12">
                    <p class="text-xl text-gray-600">Launch details could not be loaded.</p>
                    <a href="/launches"
                        class="mt-4 inline-block bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded-full">Back
                        to Launches</a>
                </section>
                <% } %>
    </main>

    <footer class="bg-gray-800 text-white text-center p-4 mt-auto">
        <p>&copy; 2025 Marcell Nemes</p>
    </footer>
</body>

</html>